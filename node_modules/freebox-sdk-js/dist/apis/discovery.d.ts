import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";
export interface Discovery {
    uid: string;
    device_name: string;
    api_version: string;
    api_base_url: string;
    device_type: string;
    api_domain: string;
    https_available: boolean;
    https_port: number;
}
export interface DiscoveryResponse extends AxiosResponse {
    data: Discovery;
}
export declare function discovery(axiosInstance?: AxiosInstance | AxiosRequestConfig): Promise<DiscoveryResponse>;
